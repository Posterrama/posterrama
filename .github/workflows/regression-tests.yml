name: Regression Tests (Disabled - Requires Real Credentials)

# ℹ️ This workflow is disabled because regression tests require:
# - Real Plex API tokens
# - Real Jellyfin API keys  
# - Real TMDB API keys
# - Real MQTT brokers
# 
# These tests should be run LOCALLY with private/test-credentials.json
# 
# To run locally:
#   npm run test:regression:contracts
#   npm run test:regression:config
#   npm run test:regression:external

on:
    workflow_dispatch:
        # Only manual trigger available
        inputs:
            note:
                description: 'Why are you running this? (Tests will fail without real credentials)'
                required: true

permissions:
    contents: read

jobs:
    notice:
        runs-on: ubuntu-latest

        steps:
            - name: Display Notice
              run: |
                  echo "⚠️  REGRESSION TESTS DISABLED ON GITHUB"
                  echo ""
                  echo "These tests require real credentials that cannot be stored on GitHub:"
                  echo "  - Plex API tokens"
                  echo "  - Jellyfin API keys"
                  echo "  - TMDB API keys"
                  echo "  - MQTT broker credentials"
                  echo ""
                  echo "✅ To run regression tests locally:"
                  echo "   1. Ensure private/test-credentials.json is configured"
                  echo "   2. Run: npm run test:regression:contracts"
                  echo "   3. Run: npm run test:regression:config"
                  echo "   4. Run: npm run test:regression:external"
                  echo ""
                  echo "📝 See private/README.md for credential setup instructions"
                  echo ""
                  echo "ℹ️  GitHub CI only runs quality checks (ESLint, Prettier, Security Audit)"
                  exit 1
