name: Gitea Actions Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Test runner
        run: |
          echo "🎉 Gitea Actions runner werkt!"
          echo "Node versie: $(node --version)"
          echo "NPM versie: $(npm --version)"
      
      - name: Quick syntax check
        run: |
          echo "✅ Basis syntax check..."
          node -e "console.log('JavaScript syntax OK')"
